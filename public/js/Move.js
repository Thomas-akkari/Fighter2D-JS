function Move(config){
    
    //Animation + Hitboxes
    this.anim = config.anim;
    this.frames = config.frames;
    
    //Meta
    this.FAF = config.FAF;
    this.landing_lag = config.landing_lag;
}