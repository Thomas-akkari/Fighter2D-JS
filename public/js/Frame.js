function Frame(config){
    
    //Hitboxes
    this.hitboxes = config.hitboxes
    
    //Meta
    this.autocancel = config.autocancel;
    this.intangible = config.intangible; // may not be used
    this.invincible = config.invincible;
    this.super_armor = config.super_armor;
    this.heavy_armor = config.heavy_armor; // based on kb or damage, will see?
    
}